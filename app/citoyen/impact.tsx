import { View, Text } from 'react-native';

export default function impact() {  // ← EXPORT DEFAULT
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Page Impact 📊</Text>
    </View>
  );
}
